Add this line to the scripts section in backend/package.json:

"seed-rules": "npx ts-node src/scripts/seedRules.ts",

Full location:
  "scripts": {
    ...existing scripts...,
    "seed-rules": "npx ts-node src/scripts/seedRules.ts",
  }
